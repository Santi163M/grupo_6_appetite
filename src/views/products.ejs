<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de productos</title>

    <link rel="stylesheet" href="css/products.css">
</head>

<body>
    <%- include('partials/header') %>
      
       <main>
        <section class="productos">
            <form action="/products/crear/<%= products.id %>" method="get">
                <button class="crear-producto">Crear nuevo producto</button>
            </form>

            <% for (let i = 0; i < products.length; i++){ %>
                <article>        
                    <div class="datos">
                        <h3>ID: <%= products[i].id %></h3>
                        <h3>Nombre: <%= products[i].nombre %></h3>
                        <h3>Categoría: <%= products[i].categoria.nombre %></h3>
                        <h3>Descripción: <%= products[i].descripcion %></h3>
                        <h3>Precio: $<%= products[i].precio %></h3>
                    </div>

                    <div class="botones">
                        <form action="/products/<%= products[i].id %>" method="GET">
                            <button type="submit">Ver detalles</button>
                        </form>

                        <form action="/products/editar/<%= products[i].id %>" method="get">
                            <button type="submit">Editar producto</button>
                        </form>

                        <form action="/products/eliminar/<%= products[i].id %>?_method=DELETE" method="post">
                            <button type="submit">Eliminar producto</button>
                        </form>
                    </div>
                </article>
            <% } %>

        </section>
    </main> 
    <%- include('partials/footer') %>
</body>

</html>