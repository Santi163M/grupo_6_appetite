<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de productos</title>

    <link rel="stylesheet" href="css/products.css">
</head>

<body>
    <%- include('partials/header') %>

    <main>
        <section class="productos">
            <form action="/productos/crear/<%= id %>" method="get">
                <button class="crear-producto">Crear nuevo producto</button>
            </form>

            <% for (producto of datosProductos) { %>
                <article>
                    <div class="img">
                        <img src="<%= producto.img %>" alt="">
                    </div>
                    
                    <div class="datos">
                        <h3>ID: <%= producto.id %></h3>
                        <h3>Nombre: <%= producto.nombre %></h3>
                        <h3>Categoría: <%= producto.categoria %></h3>
                        <h3>Descripción: <%= producto.descripcion %></h3>
                        <h3>Precio: $<%= producto.precio %></h3>
                    </div>

                    <div class="botones">
                        <form action="/products/<%= producto.id %>" method="GET">
                            <button type="submit">Ver detalles</button>
                        </form>

                        <form action="/products/editar/<%= producto.id %>" method="get">
                            <button type="submit">Editar producto</button>
                        </form>

                        <form action="/products/eliminar/<%= producto.id %>?_method=DELETE" method="post">
                            <button type="submit">Eliminar producto</button>
                        </form>
                    </div>
                </article>
            <% } %>
        </section>
    </main>

    <%- include('partials/footer') %>
</body>

</html>